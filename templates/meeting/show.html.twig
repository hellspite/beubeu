{% extends 'base.html.twig' %}

{% block title %}{{ meeting.title }} - {{ parent() }}{% endblock %}

{% block content %}
<div class="container exhibit-container">
    <div class="exhibit-title"><h1>{{ meeting.title }}</h1></div>
    <div class="exhibit-image"><img src="{{ asset('images/' ~ meeting.image) }}" class="img-responsive" /></div>
    <div class="exhibit-desc">
        {{ meeting.description|raw }}
    </div>

</div>
{% endblock %}
